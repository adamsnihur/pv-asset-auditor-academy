{
  "name": "pv-asset-auditor-academy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "node --test test/*.test.mjs",
    "verify:dist": "node scripts/verify-dist.mjs",
    "check": "npm test && npm run build && npm run verify:dist"
  },
  "dependencies": {
    "astro": "7.2.10",
    "katex": "0.18.7",
    "marked": "18.0.13",
    "three": "0.186.0"
  }
}
